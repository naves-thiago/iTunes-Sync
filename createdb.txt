create table music (
name varchar(150),
album varchar(150),
albumArtist varchar(150),
albumRating integer,
artist varchar(150),
bitRate integer,
bookmark double, 
bookmarkable boolean,
bpm integer,
category varchar(150),
comment varchar(500),
compilation boolean,
composer varchar(150),
databaseID integer,
objectDescription varchar(150),
discCount integer,
discNumber integer,
enabled boolean,
episodeID varchar(150),
episodeNumber integer,
EQ varchar(150),
finish double,
gapless boolean,
genre varchar(150),
grouping varchar(150),
longDescription varchar(500),
lyrics varchar(500),
playedCount integer,
playedDate char(25),
rating integer,
seasonNumber integer,
shufflable boolean,
skippedCount integer,
skippedDate char(25),
show varchar(150),
sortAlbum varchar(150),
sortArtist varchar(150),
sortAlbumArtist varchar(150),
sortName varchar(150),
sortComposer varchar(150),
sortShow varchar(150),
start double,
trackCount integer,
trackNumber integer,
unplayed boolean,
volumeAdjustment integer,
year integer);

create table diff_music (
name varchar(150),
album varchar(150),
albumArtist varchar(150),
albumRating integer,
artist varchar(150),
bitRate integer,
bookmark double, 
bookmarkable boolean,
bpm integer,
category varchar(150),
comment varchar(500),
compilation boolean,
composer varchar(150),
databaseID integer,
objectDescription varchar(150),
discCount integer,
discNumber integer,
enabled boolean,
episodeID varchar(150),
episodeNumber integer,
EQ varchar(150),
finish double,
gapless boolean,
genre varchar(150),
grouping varchar(150),
longDescription varchar(500),
lyrics varchar(500),
playedCount integer,
playedDate char(25),
rating integer,
seasonNumber integer,
shufflable boolean,
skippedCount integer,
skippedDate char(25),
show varchar(150),
sortAlbum varchar(150),
sortArtist varchar(150),
sortAlbumArtist varchar(150),
sortName varchar(150),
sortComposer varchar(150),
sortShow varchar(150),
start double,
trackCount integer,
trackNumber integer,
unplayed boolean,
volumeAdjustment integer,
year integer);

create table diff_date (
music char(25) );
